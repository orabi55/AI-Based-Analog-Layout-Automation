# flow Implementation Plan

## [Hard] Phase 1 - Priority 1: AI-Assisted flow by editing on an existing symbolic layout (e.g. after initial placement) over multi...
**Full Feature:** AI-Assisted flow by editing on an existing symbolic layout (e.g. after initial placement) over multiple sessions
Adjust grouping, relative placement, dimensions of the groups, devices locations, matching patterns, rules inside groupls, rules among groups

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Medium] Phase 1 - Priority 1: Find a way to save device information (fingers, multipliers ,no. of fins, etc..) while moving from a...
**Full Feature:** Find a way to save device information (fingers, multipliers ,no. of fins, etc..) while moving from and to CC

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Medium] Phase 1 - Priority 1: Initial placement with no contraints/single dimension constraint
(consider matching patterns, logica...
**Full Feature:** Initial placement with no contraints/single dimension constraint
(consider matching patterns, logical order by main signal flow, limited area optimization without changing arrangement)

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Hard] Phase 1 - Priority 1: Load an existing layout and catch placement groups, patterns, relative placement, .. etc...
**Full Feature:** Load an existing layout and catch placement groups, patterns, relative placement, .. etc

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Hard] Phase 2 - Priority 1: Extract design description from a reference design...
**Full Feature:** Extract design description from a reference design

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Hard] Phase 2 - Priority 1: Initial Placement with a given reference file (only description that specify matching, relative plac...
**Full Feature:** Initial Placement with a given reference file (only description that specify matching, relative placement, some dimensions constraints, .. etc)

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

## [Medium] Phase 3 - Priority 1: SCs block including adding fillers, decap, endings, taps ..etc...
**Full Feature:** SCs block including adding fillers, decap, endings, taps ..etc

### Technical Implementation Steps:
1. **Architectural Design:** Define the data flow between modules (EDA -> Parser -> Placement -> GUI).
2. **Core Logic Development:** Implement the central algorithms and state management.
3. **Abstraction Layers:** Create generic interfaces to handle multiple technologies or cell types.
4. **End-to-End Testing:** Validate the complete flow from initial schematic/reference to final layout.

---

